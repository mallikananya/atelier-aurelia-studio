{
  "name": "atelier-aurelia-studio",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@atelier-aurelia/studio",
    "infra:start": "docker compose --env-file .env -f infra/compose.yml up -d",
    "infra:stop": "docker compose --env-file .env -f infra/compose.yml down",
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:test": "supabase test db",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "node --test tests/*.test.mjs && npm run test --workspaces --if-present",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=80 --test-coverage-functions=80 --test-coverage-branches=80 tests/*.test.mjs && npm run test:coverage --workspaces --if-present",
    "lint": "eslint tests eslint.config.mjs tools && npm run lint --workspaces --if-present",
    "guardrails": "node tools/architecture-guardrails.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.55.0",
    "@vitest/coverage-v8": "^5.0.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "supabase": "^2.39.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^5.0.0"
  }
}
